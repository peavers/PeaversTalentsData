name: Package and Release

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *'

permissions:
  contents: write
  actions: write

jobs:
  package-and-release:
    uses: peavers-warcraft/workflow-templates/.github/workflows/packaging.yml@master
    with:
      use_self_hosted: false
    secrets: inherit
